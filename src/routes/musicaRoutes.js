const express = require('express');
const router = express.Router();
const controller = require('../cpntroller/musicaController')

router.get("/", controller.getAll);
router.get("/musica, controller.getAll)");

modele.exports = router;